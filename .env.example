APP_ENV=production
APP_DEBUG=false
APP_URL=
APP_TIMEZONE=Europe/Moscow
SESSION_NAME=chess_rewards_session

DB_HOST=localhost
DB_PORT=3306
DB_DATABASE=chess_rewards
DB_USERNAME=database_user
DB_PASSWORD=database_password
DB_CHARSET=utf8mb4

# Enable only during first setup, then switch back to false.
INSTALLER_ENABLED=false
